[phases.setup]
nixPkgs = ["...", "php82", "composer"]

[phases.build]
cmds = ["composer install", "php artisan key:generate", "php artisan migrate --force", "php artisan storage:link"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
